let height = 10 ; 
let base = 4 ; 
let perimiter = 2 * (height + base); 
let area = height * base;
let messagePerimitar = `The Rectangular Perimiter is ${perimiter} and sides length is: height- ${height} base- ${base}`;
let messageArea = `The Rectangular Area is ${area} and sides length is: height- ${height} base- ${base}`;

console.log(messagePerimitar);
console.log(messageArea);










